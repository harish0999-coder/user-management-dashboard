// Global test setup, loaded once before the suite via vite.config.js
// `test.setupFiles`. Adds the jest-dom matchers (toBeInTheDocument, etc.)
// to every test file automatically.
import "@testing-library/jest-dom";
